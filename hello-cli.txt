Hello CLI!

Remember, you can use such commands as

$ git remote add origin <remote_url>
$ git push origin master
$ git add <filename>
$ git commit -m "explanation"
$ git status

For branches

$ git checkout <branch>
$ git checkout master
$ git merge clean_up

Cool stuff!!

